# Reserved UI Surface

This folder is intentionally reserved for the next Canvas UI layer.

The current View-built primitives were moved to:

- `src/lib/components/view-ui`

That keeps the old implementation available while freeing `src/lib/components/ui`
for a new reference-library-based component surface.
