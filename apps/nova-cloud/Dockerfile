# Legacy sandbox image placeholder.
# Nova Cloud no longer uses Cloudflare Sandbox as the app runtime.
FROM node:24-bookworm-slim

# Expose common local development ports.
EXPOSE 8000
EXPOSE 8080
EXPOSE 5173
