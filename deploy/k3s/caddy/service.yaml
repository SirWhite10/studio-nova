apiVersion: v1
kind: Service
metadata:
  name: caddy
  namespace: nova-edge
  labels:
    app.kubernetes.io/name: caddy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: caddy
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: admin
      port: 2019
      targetPort: admin
