apiVersion: v1
kind: Service
metadata:
  name: smoke-workspace
  namespace: nova-edge
  labels:
    app.kubernetes.io/name: smoke-workspace
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: smoke-workspace
  ports:
    - name: http
      port: 3000
      targetPort: http
